---
- name: set router banners
  hosts: routers
  gather_facts: no

  tasks:
    - name: load banner onto network device
      vars:
        - network_banner:  "{{ net_banner | default('Default Ansible Banner', true) }}"
        - banner_type: "{{ net_type | default('login', true) }}"
      include_role:
        name: banner

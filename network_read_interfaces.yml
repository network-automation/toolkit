---
- name: configure l3 interface
  hosts: routers
  gather_facts: no

  tasks:

    - name: load l3 interface role
      include_role:
        name: read_interfaces

    - name: load l3 interface role
      debug:
        msg: "{{interfaces_info}}"
